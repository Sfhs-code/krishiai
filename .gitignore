# Dependencies
node_modules/

# Build output
dist/
# Scoped to the functions build dir — a bare `lib/` would also swallow
# web/src/lib, which is source.
/functions/lib/
*.tsbuildinfo

# Secrets — never commit API keys
.env
.env.*
!.env.example
*.local

# Firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*
*-debug.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
